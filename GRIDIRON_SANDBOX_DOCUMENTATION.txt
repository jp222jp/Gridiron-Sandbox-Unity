🗒️ Master System Documentation ArchiveSave the following comprehensive summary block as a separate text file named GRIDIRON_SANDBOX_DOCUMENTATION.txt on your desktop. This file is specifically optimized for future AI sessions to ingest, ensuring they understand the custom logic we built without introducing bugs or breaking constraints.🏈 GRIDIRON SIMULATION SANDBOX ARCHITECTURE SPECIFICATIONVersion: 4.2 (Modular Architecture Commit)Target Engine: Unity (Modern Input System API)External Backend Integration: Python JSON Spreadsheet Matrix📌 PART 1: MASTER CONTEXT & CORE DESIGN PHILOSOPHYThis project is a physics-driven, anatomy-grounded football simulation sandbox designed to escape legacy "arcade stat rolls" by calculating on-field outcomes through actual biomechanical vectors, human mass tolerances, and cognitive computational bandwidth.Every entity on the field is derived from a unified Master Prefab template (Football_Player) whose code behaviors branch dynamically frame-by-frame depending on a single inspector boolean checkbox: isUserControlled.TRUE (The Player): Intercepts live gamepad inputs, controls first-person helmet POV camera vectors, and drives movement tracking relative to the face mask viewpoint orientation.FALSE (The CPU Drone/Dummy): Bypasses gamepad event listeners entirely to prevent input hijacking errors. Purges nested visual cameras to keep memory overhead light, sitting in a passive state awaiting vector instructions or collision checking.📐 PART 2: THE DECOUPLED COMPONENT ARCHITECTURETo maintain clean, scalable code segregation, the actor archetype is split across four independent C# modules attached to a single capsule object. They communicate on frame one via cached GetComponent handshakes:1. PlayerAttributes.cs (Scouted Combine Matrix)Houses all primitive numeric values loaded from the database. It contains four core categories:Biomechanical Stature: Height/Width inches, target vertical takeoff caps, and arm wingspan reaches.Kinetic Strength: Segregated into Upper Body (extensions/jams), Core Body (balance recovery/turn stabilization), and Lower Body (takeoff impulses/linear drives).Neuromuscular Vectors: quickTwitchExplosiveness (Muscle firing speed on the first 3 frames) and reactiveAgility (Energy retention on sharp directional cuts).Cognitive Constraints: calculationsPerSecondX (Computational focus bandwidth) and spatialResolutionRegions (Field grid chunking resolution depth).2. PlayerAnatomyRig.cs (The 10-Point Proxy Skeleton)Simulates human anatomy on featureless capsule cylinders by maintaining an invisible nested folder hierarchy:Upper Links: Proxy_Shoulder_Left/Right ➔ Proxy_Elbow_Left/Right ➔ Proxy_Hand_Left/RightLower Links: Proxy_Hip_Left/Right ➔ Proxy_Knee_Left/Right ➔ Proxy_Foot_Left/RightBehavior: On frame one, this script grabs these empty objects and transforms their local coordinate positions using real player stature inches (e.g., projecting hand markers forward exactly 34.5 inches past the shoulder span width).3. PlayerBiomechanicalFailure.cs (The Stress & Injury Engine)Calculates real-time structural fatigue based on the player’s mass, angular velocity, and personal choices:Route Sharpness Bias: Scales from 0.2 (wide, rounded curvilinear tracking arcs) up to 1.0 (jagged, hard-angle snaps).Fatigue Accumulator: Running straight cools the body down. Sharp, high-speed twisting maneuvers inject kinetic strain frames into an active accumulator.Conditional Failures: Runs frame-by-frame probability checks that are conditioned on attributes. If turning strain exceeds Core Stability + Cleat Friction limits, a Slip Event occurs (injecting a sliding lateral drift vector). If the accumulator passes a customized durability threshold, a Soft-Tissue Injury triggers, collapsing the upright capsule column constraints instantly onto the turf.4. PlayerPhysicsController.cs (The Master Central Hub)The core vector compiler that drives physical execution loops inside FixedUpdate():Pre-Play Delays: Combines schematicRecognition and playbookFamiliarity percentages to calculate an artificial neural reaction delay on frame one, freezing foot movement for a set number of frames to simulate a player "thinking too much" instead of playing fast.Cognitive Bandwidth Optimization (The X Variable): Pushing a thumbstick does not instantly snap movement paths. Instead, the script uses a timer interval based on 1.0 / calculationsPerSecondX to snapshot the user's intent vector. In between those ticks, the capsule smoothly curves toward that target using its strength profiles, creating fluid, organic steering paths.Relative Strafe Locomotion: Squeezing LT down locks your face angles downfield. Pushing Forward on the left stick translates your capsule linear velocity relative to your local visor line (running straight toward whatever direction your face mask bars point).Strength-to-Weight Takeoffs: Tapping Y solves for target height and scales the takeoff force proportional to body weight mass (\(F = m \cdot \sqrt{2gh}\)), multiplied by athletic efficiency constants.🎛️ PART 3: RE-INTEGRATION HARDWARE SHORTCUTSThe Cheat-Code Heal (Xbox Back Button / Key 'R'): Instantly wipes all fatigue accumulators, clears the isInjured boolean, snaps the capsule back upright, restores rigid vertical rotation axis constraints, and fully restores player control fluidly.The Direct Data Pipeline: Unity's built-in JsonUtility parses an external player_database.json document inside the reserved Assets/StreamingAssets/ directory. It reads the string key typed inside an object's Database Player ID inspector bar and overwrites all fields instantly on frame one.⚠️ PART 4: CRITICAL RE-ENTRANCY WARNINGS FOR THE NEXT AIDo NOT restore legacy input axes: Under no circumstances should lines like Input.GetAxis("LeftTrigger") or Input.GetButtonDown("Jump") be reintroduced. They cause unhandled ArgumentException script crashes on frame one if the project session tabs are cloned. All hardware checks must pull cleanly through modern Input System Action maps (controls.Gameplay.Strafe.IsPressed()) or protected hardware checks.Protect the mathematical zero shield: When transforming vectors relative to local space (TransformDirection), ensure the code blocks screen out neutral, dead-center joystick states (magnitude < 0.1f). Passing absolute zeroes into local translation formulas crashes Unity's internal physics solver, freezing both movement and jumping mechanics.Do NOT use raw world coordinates for height overrides: The vertical leverage push-offs use relative bone proxy distance offsets (Proxy_Shoulder_Left.position.y). Hard-coding raw coordinate variables will break clearance loops when characters scale weights or jump dynamically.Keep Rigidbody position Y unchecked: Ensure Freeze Position Y remains unchecked inside the player's Rigidbody constraints dropdown, while Freeze Rotation X and Z remain checked to keep the capsule standing tall without locking out vertical lift forces.